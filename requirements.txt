# Web framework
fastapi==0.115.7
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# Audio processing
librosa==0.10.2.post1
soundfile==0.13.1
numpy==2.0.2

# Step 1 — Speech-to-Text via Qwen3-ASR (state-of-the-art, German, Jan 2026)
# Requires Python 3.12+ — create a new venv if on older Python
qwen-asr>=0.1.0
torch>=2.6.0

# Step 2 — LLM German conversation via Groq (Llama-3.3-70b)
groq>=0.9.0

# Step 3 — German Text-to-Speech via Microsoft Edge TTS (free, no API key)
edge-tts>=6.1.9

# Environment variable management (.env file loading)
python-dotenv>=1.0.0

# Database (SQLite via SQLAlchemy ORM)
sqlalchemy>=2.0.0

# Authentication (JWT + bcrypt password hashing)
PyJWT>=2.8.0
bcrypt==4.0.1
