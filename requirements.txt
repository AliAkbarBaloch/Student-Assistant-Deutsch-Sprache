# Core
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0

# HuggingFace / PyTorch
# torch 2.2.2 is the last release with Intel Mac (x86_64) wheels
torch==2.2.2
numpy<2
# transformers 4.40.x is the last series compatible with torch 2.2.x
# and already includes full Llama 3 support (added in 4.38)
transformers==4.40.2
accelerate>=0.29.0,<0.31.0
huggingface_hub>=0.23.0
sentencepiece>=0.2.0     # tokenizer for Llama 3

# Optional: 4-bit quantisation (requires CUDA)
# bitsandbytes>=0.43.0
